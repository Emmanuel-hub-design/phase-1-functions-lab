function distanceFromHqInBlocks() {
    const hqLocation = 42;
    const distanceBlocks = Math.abs(50 -42);
    return distanceBlocks;
  }
  function distanceFromHqInFeet(someValue) {
    distanceFromHqInBlocks(8);
    const distanceInBlocks = distanceFromHqInBlocks(8);
    const feetPerBlock = 264;
    const distanceInFeet = distanceInBlocks * feetPerBlock;
    return distanceInFeet;
    
    
  }
  function distanceTravelledInFeet(start, destination) {

    const distance = distanceTravelledInFeet(34, 38);
    const blockLengthInFeet = 264;
    const distanceInFeet = (destination - start) * blockLengthInFeet;
    return distanceInFeet;
    console.log(distance);
  }
  
  